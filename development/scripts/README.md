# Development Scripts

Legacy Python scripts for setting up teams and matches via the API. These are no longer used as tournament data is now seeded via Flyway migrations (see `lambdas/src/main/resources/db/migration/`).
